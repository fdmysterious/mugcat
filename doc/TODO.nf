TODO-List
	Template & Code Structure
		( ) Fack*ng resolve issues (making absolute paths work)
		( ) Better format to write pages (Some md-enabled flavor ?)
			( ) Page format
			( ) Portfolio items format
			( ) Auto route generation
	
	Website fancyness
		( ) Opengraph tags
		( ) JSON+LD Schema.org

	Triangles animation
		( ) Avoid coincident points
